# Backend runtime + test deps. Kept deliberately minimal to shrink the
# audit/attack surface. Pin majors; review every addition for security impact.
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
cryptography==43.*   # server-side Ed25519 verification (login + ownership proof)
websockets==13.*   # used only by the local smoke-test client
pytest==8.*
pytest-asyncio==0.24.*
httpx==0.27.*       # ASGI test client for endpoint tests
