# Backend runtime + test deps. Kept deliberately minimal to shrink the
# audit/attack surface. EXACT pins (audit 2026-07-18 L-03): an upgrade is a
# deliberate, reviewed act, and requirements.lock (pip-compile
# --generate-hashes) is the hash-verified set actually installed on deploy.
# Security floors baked into these pins:
#   starlette >= 1.0.1  — host-header URL desync (GHSA-86qp-5c8j-p5mr, M-01)
#   starlette >= 0.49.1 — Range-header quadratic DoS (CVE-2025-62727, M-02)
#   cryptography >= 48.0.1 — GHSA-537c-gmf6-5ccf (L-03)
fastapi==0.139.2
starlette==1.3.1
uvicorn[standard]==0.51.0
pydantic==2.13.4
cryptography==49.0.0   # server-side Ed25519 verification (login + ownership proof)
dilithium-py==1.4.0    # server-side ML-DSA-65 verification (PQ ownership proof at registration)
websockets==16.1.1     # used only by the local smoke-test client
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1          # ASGI test client for endpoint tests
